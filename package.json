{
  "name": "medical-records",
  "version": "1.0.0",
  "description": "Offline Medical Records System",
  "scripts": {
    "install-all": "cd frontend && npm install",
    "build": "cd frontend && npm run build",
    "dev": "cd frontend && npm start"
  }
}
